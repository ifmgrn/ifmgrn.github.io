# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-commit:
  commands:
    ts:
      glob: "*.ts"
      run: pnpm -s run check
    check:
      run: pnpm -s run lint {staged_files}
      stage_fixed: true